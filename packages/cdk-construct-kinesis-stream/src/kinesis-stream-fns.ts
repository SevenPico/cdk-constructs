import { Context } from '@sevenpico/cdk-context';

export interface KinesisStreamOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const kinesisStreamProps = (ctx: Context, opts: KinesisStreamOptions): Record<string, unknown> => ({
  // TODO: implement
});
