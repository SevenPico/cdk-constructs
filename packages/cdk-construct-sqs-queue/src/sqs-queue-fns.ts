import { Context } from '@sevenpico/cdk-context';

export interface SqsQueueOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const sqsQueueProps = (ctx: Context, opts: SqsQueueOptions): Record<string, unknown> => ({
  // TODO: implement
});
