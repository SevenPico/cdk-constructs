import { Context } from '@sevenpico/cdk-context';

export interface StepFunctionsOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const stepFunctionsProps = (ctx: Context, opts: StepFunctionsOptions): Record<string, unknown> => ({
  // TODO: implement
});
