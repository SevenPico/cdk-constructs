import { Context } from '@sevenpico/cdk-context';

export interface CloudtrailOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const cloudtrailProps = (ctx: Context, opts: CloudtrailOptions): Record<string, unknown> => ({
  // TODO: implement
});
