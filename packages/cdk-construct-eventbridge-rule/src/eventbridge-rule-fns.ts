import { Context } from '@sevenpico/cdk-context';

export interface EventbridgeRuleOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const eventbridgeRuleProps = (ctx: Context, opts: EventbridgeRuleOptions): Record<string, unknown> => ({
  // TODO: implement
});
