import { Context } from '@sevenpico/cdk-context';

export interface CloudwatchEventsOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const cloudwatchEventsProps = (ctx: Context, opts: CloudwatchEventsOptions): Record<string, unknown> => ({
  // TODO: implement
});
