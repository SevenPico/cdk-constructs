import { Context } from '@sevenpico/cdk-context';

export interface S3WebsiteOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const s3WebsiteProps = (ctx: Context, opts: S3WebsiteOptions): Record<string, unknown> => ({
  // TODO: implement
});
