import { Context } from '@sevenpico/cdk-context';

export interface SecretOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const secretProps = (ctx: Context, opts: SecretOptions): Record<string, unknown> => ({
  // TODO: implement
});
