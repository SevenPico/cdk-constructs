import { Context } from '@sevenpico/cdk-context';

export interface S3BucketOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const s3BucketProps = (ctx: Context, opts: S3BucketOptions): Record<string, unknown> => ({
  // TODO: implement
});
