import { Context } from '@sevenpico/cdk-context';

export interface LambdaErrorNotificationOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const lambdaErrorNotificationProps = (ctx: Context, opts: LambdaErrorNotificationOptions): Record<string, unknown> => ({
  // TODO: implement
});
