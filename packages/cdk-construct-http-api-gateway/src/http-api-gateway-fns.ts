import { Context } from '@sevenpico/cdk-context';

export interface HttpApiGatewayOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const httpApiGatewayProps = (ctx: Context, opts: HttpApiGatewayOptions): Record<string, unknown> => ({
  // TODO: implement
});
