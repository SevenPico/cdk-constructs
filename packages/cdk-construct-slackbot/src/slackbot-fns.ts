import { Context } from '@sevenpico/cdk-context';

export interface SlackbotOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const slackbotProps = (ctx: Context, opts: SlackbotOptions): Record<string, unknown> => ({
  // TODO: implement
});
