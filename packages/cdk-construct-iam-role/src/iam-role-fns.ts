import { Context } from '@sevenpico/cdk-context';

export interface IamRoleOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const iamRoleProps = (ctx: Context, opts: IamRoleOptions): Record<string, unknown> => ({
  // TODO: implement
});
