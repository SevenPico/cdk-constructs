import { Context } from '@sevenpico/cdk-context';

export interface DynamodbOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const dynamodbProps = (ctx: Context, opts: DynamodbOptions): Record<string, unknown> => ({
  // TODO: implement
});
