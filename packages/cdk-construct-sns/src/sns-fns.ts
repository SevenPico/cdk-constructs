import { Context } from '@sevenpico/cdk-context';

export interface SnsOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const snsProps = (ctx: Context, opts: SnsOptions): Record<string, unknown> => ({
  // TODO: implement
});
