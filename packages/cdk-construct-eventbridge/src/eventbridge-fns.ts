import { Context } from '@sevenpico/cdk-context';

export interface EventbridgeOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const eventbridgeProps = (ctx: Context, opts: EventbridgeOptions): Record<string, unknown> => ({
  // TODO: implement
});
