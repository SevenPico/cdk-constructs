import { Context } from '@sevenpico/cdk-context';

export interface LambdaFunctionOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const lambdaFunctionProps = (ctx: Context, opts: LambdaFunctionOptions): Record<string, unknown> => ({
  // TODO: implement
});
