import { Context } from '@sevenpico/cdk-context';

export interface RedshiftClusterOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const redshiftClusterProps = (ctx: Context, opts: RedshiftClusterOptions): Record<string, unknown> => ({
  // TODO: implement
});
