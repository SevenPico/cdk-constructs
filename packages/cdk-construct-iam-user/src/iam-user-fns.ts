import { Context } from '@sevenpico/cdk-context';

export interface IamUserOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const iamUserProps = (ctx: Context, opts: IamUserOptions): Record<string, unknown> => ({
  // TODO: implement
});
