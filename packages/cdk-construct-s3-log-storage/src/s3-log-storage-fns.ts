import { Context } from '@sevenpico/cdk-context';

export interface S3LogStorageOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const s3LogStorageProps = (ctx: Context, opts: S3LogStorageOptions): Record<string, unknown> => ({
  // TODO: implement
});
