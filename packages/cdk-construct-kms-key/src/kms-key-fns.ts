import { Context } from '@sevenpico/cdk-context';

export interface KmsKeyOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const kmsKeyProps = (ctx: Context, opts: KmsKeyOptions): Record<string, unknown> => ({
  // TODO: implement
});
