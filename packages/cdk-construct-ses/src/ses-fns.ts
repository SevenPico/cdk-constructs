import { Context } from '@sevenpico/cdk-context';

export interface SesOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const sesProps = (ctx: Context, opts: SesOptions): Record<string, unknown> => ({
  // TODO: implement
});
