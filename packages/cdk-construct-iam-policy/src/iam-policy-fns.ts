import { Context } from '@sevenpico/cdk-context';

export interface IamPolicyOptions {
  // TODO: define options
}

// Pure functions — no side effects, no CDK constructs
export const iamPolicyProps = (ctx: Context, opts: IamPolicyOptions): Record<string, unknown> => ({
  // TODO: implement
});
